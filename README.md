# TinyLSM Lab UI

Local React and TypeScript frontend for exploring TinyLSM operations, internal
state, persistent files, workloads, and recovery behavior.

## Development

```sh
npm install
npm run dev
```

Vite proxies `/api` requests to the future C++ lab server at
`http://127.0.0.1:8080`.

